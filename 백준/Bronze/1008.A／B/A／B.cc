#include <bits/stdc++.h>
using namespace std;

void solve(){

    double a, b;
    cin >> a >> b;
    cout << std::fixed;
    cout << std::setprecision(10);
    cout <<  a/b;
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}
