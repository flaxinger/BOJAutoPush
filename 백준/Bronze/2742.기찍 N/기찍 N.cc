#include <bits/stdc++.h>
using namespace std;

void solve(){
    
    int a;
    cin >> a;

    for(int i = a; i >0; i--) cout << i << '\n';
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}
